import Vue from 'vue';
import Vuetify from 'vuetify/lib';
import 'vuetify/dist/vuetify.min.css'   // Use full installation, not cli a la carte

Vue.use(Vuetify, {
  iconfont: 'md',
  theme: {
    primary: '#4d648d'
  }
});
